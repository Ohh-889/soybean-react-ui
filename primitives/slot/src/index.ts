export { createSlotClone } from './clone';

export { createSlot, createSlottable, Slot, Slot as Root, Slottable } from './create';

export type { SlotProps } from './create';

export { getElementRef, mergeProps } from './shared';
